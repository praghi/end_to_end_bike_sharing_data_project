# end_to_end_bike_sharing_data_project
This project demonstrates a complete data engineering and advance analytics using the UCI Bike Sharing dataset.It follows the medallion architecture — with Bronze (raw), Silver (cleaned), and Gold (curated) layers — and integrates CI/CD to automatically deploy from the test environment to production.
